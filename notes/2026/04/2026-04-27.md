# 2026-04-27 Session Notes

## Scope
- Deliver GitHub issue #12 for generic observability patterns in the template.
- Deliver GitHub issue #8 for the release deployment skeleton.

## Issue #12

### Implementation
- Added generic structured logging helpers in `src/logging_config.py` with stderr plus optional JSONL output and `contextvars` correlation fields.
- Added `src/release_info.py` with env -> git -> `VERSION` fallback metadata lookup.
- Wired startup logging in `src/main.py`.
- Added `docs/OBSERVABILITY.md` and `docs/DEPLOYMENT.md`.
- Added Hetzner systemd template skeletons for app, job, and timer units.
- Extended `config/config.example.yaml`, `docs/INDEX.md`, and `AGENTS.md` for observability guidance.
- Updated `setup_template.py` to process the new files and render placeholder-bearing template paths during setup.
- Added tests for logging and release metadata plus a test helper for source-package discovery after `SOURCE_DIR` renames.

### Validation
- `git diff --check`
- `python3 -m py_compile src/main.py src/logging_config.py src/release_info.py tests/module_loader.py tests/test_main.py tests/test_logging_config.py tests/test_release_info.py setup_template.py`
- Focused Python smoke checks for `JSONFormatter`, `configure_logging()`, `get_release_info()`, and `rename_template_paths()`
- `pytest` and `pre-commit` were not available in the shell, so they were not run.

### PR / Merge Log
- Branch: `feature/12-observability-patterns`
- PR: #19
- Merged to `main` as `c237a4e`
- Review feedback fixed in follow-up commit `aabeb5c`

## Follow-Up Issues Created
- [followup] #20 Make template workflow files valid before rendering

## Issue #8

### Implementation
- Added `.github/workflows/release.yml` for manual and release-event deployment orchestration with tag verification, release-body deployment status updates, secrets materialization, SSH setup, and smoke-check hooks.
- Added `scripts/github/resolve_release_context.py` to normalize `workflow_dispatch` and `release` events into stable deployment inputs.
- Added `scripts/redeploy.sh` and `scripts/release_smoke_check.sh` as deployment and post-deploy smoke-check wrappers.
- Added Ansible skeletons and example inventory/secrets files under `infra/hetzner/`, `infra/home-worker/`, and `infra/site/`.
- Added `docs/RELEASE_WORKFLOW.md` and expanded `docs/DEPLOYMENT.md`, `docs/INDEX.md`, `docs/ARCHITECTURE.md`, `TEMPLATE_USAGE.md`, and `.gitignore` for the release/deployment flow.
- Fixed review feedback by changing release authorization to owner/admin permission checks and by deploying the published tag itself on `release` events instead of `target_commitish`.

### Validation
- `pytest -c /dev/null tests -q`
- `pytest -c /dev/null tests/test_resolve_release_context.py -q`
- `bash -n scripts/redeploy.sh scripts/release_smoke_check.sh`
- YAML parse checks for `.github/workflows/release.yml` and the new Ansible files
- `actionlint .github/workflows/release.yml`
- `pre-commit run --all-files` remained blocked in the raw template checkout because `.pre-commit-config.yaml`, `ci.yml`, and `ci-image.yml` still contain unresolved placeholders; that baseline issue remains tracked in #20

### PR / Merge Log
- Branch: `feature/8-release-pipeline-skeleton`
- PR: #21
- Review findings addressed in rebased branch commit `c60839d`
- Merged to `main` as `fb21ddc`
- Merged directly after explicit user approval despite the known raw-template workflow/config renderability blocker tracked in #20

## Issues #9 And #14

### Implementation
- Completed the remaining `AGENTS.md` / `CLAUDE.md` source-of-truth migration work and kept `CLAUDE.md` as the compatibility symlink.
- Updated `setup_template.py` to render `AGENTS.md`, recreate `CLAUDE.md -> AGENTS.md`, verify the symlink after setup, and preserve a readable fallback `CLAUDE.md` file when symlink creation fails.
- Added the missing `.mcp.json.example`, ignored `.mcp.json`, and documented MCP setup in `docs/SETUP.md` and `TEMPLATE_USAGE.md`.
- Added `ai-skills/example-skill/` as the minimal canonical skill scaffold with manifest, instructions, and stub script.
- Expanded `.claude/settings.local.json` and documented the fewer-permission-prompts workflow in `docs/SETUP.md`.
- Updated `README.md`, `docs/AI_SKILLS.md`, `TEMPLATE_USAGE.md`, and `AGENTS.md` so AGENTS is the primary guidance file and the remaining agent-skill/setup docs match the template behavior.

### Validation
- `git diff --check`
- `python3 -m py_compile setup_template.py tests/test_setup_template.py`
- Focused Python smoke checks for `render_template_path()` and both the normal and symlink-failure paths of `ensure_claude_symlink()`
- `python3 -m pytest tests/test_setup_template.py` could not run because `pytest` is not installed in this shell.
- `pre-commit run --all-files` could not run because `pre-commit` is not installed in this shell.

### PR / Merge Log
- Branch: `feature/9-14-agent-workflow-completion`
- PR: #22
- Mechanical history-preservation commit: `dc450f1`
- Follow-up review fix for symlink-failure fallback: `35c7bc7`
- Rebase-merged to `main` as `02d6b03`

## PRs #1, #2, And #3

### Implementation
- Finished the only remaining active dependency upgrade work by updating `.github/workflows/claude.yml` and `.github/workflows/claude-code-review.yml` from `actions/checkout@v4` to `actions/checkout@v6`.
- Verified that active repository workflows no longer use `actions/setup-python`, so Dependabot PR #2 was obsolete rather than incomplete work.
- Verified that `main` already uses `codecov/codecov-action@v6` in `.github/workflows/ci.yml`, so Dependabot PR #3 was obsolete rather than incomplete work.
- Closed stale Dependabot PRs #1, #2, and #3 with explicit comments tied to the merged state of `main`.

### Validation
- `git diff --check`
- `rg -n "actions/(checkout|setup-python)@|codecov/codecov-action@" .github/workflows -S`
- `pre-commit run --all-files` remained blocked in the raw template repository because `.pre-commit-config.yaml` still contains unresolved placeholders such as `{{MAX_LINE_LENGTH}}`

### PR / Merge Log
- Branch: `fix/1-2-3-workflow-action-upgrades`
- PR: #23
- Squash-merged to `main` as `f70b440`
