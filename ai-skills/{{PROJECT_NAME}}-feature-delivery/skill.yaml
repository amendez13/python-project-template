name: "{{PROJECT_NAME}}-feature-delivery"
description: Use when delivering {{PROJECT_NAME}} GitHub issues end to end through dedicated git worktree, tests, PR, CI, review, merge, and cleanup.
codex:
  interface:
    display_name: "{{PROJECT_NAME}} Feature Delivery"
    short_description: Implement {{PROJECT_NAME}} issues end to end in a worktree
    default_prompt: Use ${{PROJECT_NAME}}-feature-delivery to deliver a {{PROJECT_NAME}} GitHub issue end to end.
